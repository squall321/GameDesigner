; DesignPatterns plugin — root/anchor gameplay tags.
; Concrete channels/states/commands are authored by the GAME PROJECT as child tags
; (e.g. DP.Bus.Combat.Damage, DP.FSM.AI.Patrol). These roots only guarantee the
; hierarchy exists so parent-matches-child routing works. The roots are also declared
; as native tags in DesignPatternsNativeTags.cpp; listing them here makes them visible
; and selectable in the editor tag picker without requiring a native load.

[/Script/GameplayTags.GameplayTagsList]
+GameplayTagList=(Tag="DP.Bus",DevComment="DesignPatterns message bus channel root")
+GameplayTagList=(Tag="DP.Pool",DevComment="DesignPatterns object pool category root")
+GameplayTagList=(Tag="DP.FSM",DevComment="DesignPatterns finite state machine state root")
+GameplayTagList=(Tag="DP.Cmd",DevComment="DesignPatterns command kind/category root")
+GameplayTagList=(Tag="DP.Service",DevComment="DesignPatterns service locator key root")
+GameplayTagList=(Tag="DP.Data",DevComment="DesignPatterns data registry identity root")
+GameplayTagList=(Tag="DP.UI",DevComment="DesignPatterns UI screen/layer root")
